export interface Theme {
  colors: {
    white: string
    font: string
    fontInvert: string
    lightFont: string
    lightestFont: string
    background: string
    background40: string
    accent: string
    accent90: string
  }
  sizes: {
    borderRadius: string
  }
}
